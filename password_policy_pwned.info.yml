name: Password Policy Pwned Passwords
description: Sets up a password constraint to check if passwords are exposed through Pwned Passwords.
package: Security
type: module
core: 8.x
dependencies:
  - password_policy:password_policy
